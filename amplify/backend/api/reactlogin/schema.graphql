type Images @model @auth(rules: [{allow: public}]) {
  id: ID!
  imgUrl: String
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
}
 